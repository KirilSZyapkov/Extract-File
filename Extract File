function extractFile(input) {
    let i = input.lastIndexOf(`.`);
    let extension = input.substr(i + 1);
    let fielName = input.slice(input.lastIndexOf(`\\`)+1,i);
    console.log(`File name: ${fielName}`);
    console.log(`File extension: ${extension}`);

}
